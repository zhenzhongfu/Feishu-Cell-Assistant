import React from 'react';

// 创建Markdown基础样式
export const createMarkdownBaseStyles = (): React.CSSProperties => ({
  /* 基础样式会通过外部传入的变量应用 */
});

// Notion特有样式
export const createNotionStyles = (): React.CSSProperties => ({
  /* Notion特有样式会通过外部传入的变量应用 */
});

// GitHub特有样式
export const createGithubStyles = (): React.CSSProperties => ({
  /* GitHub特有样式会通过外部传入的变量应用 */
}); 